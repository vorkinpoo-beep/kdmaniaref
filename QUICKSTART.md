# 🚀 Быстрый запуск бота

## Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/Vorkinpoo-beep/kdmaniaref.git
cd kdmaniaref
```

## Шаг 2: Установка зависимостей

```bash
pip3 install -r requirements.txt
```

## Шаг 3: Проверка конфигурации

Откройте `config.py` и убедитесь, что все настройки правильные:
- `BOT_TOKEN` - токен вашего бота
- `CHANNEL_ID` - ID канала (-1001959515253)
- `ADMIN_ID` - ваш Telegram ID (1617188857)

## Шаг 4: Запуск бота

### Вариант A: Обычный запуск (для тестирования)

```bash
python3 main.py
```

### Вариант B: Запуск в фоне через screen (рекомендуется для VNC)

```bash
# Установите screen (если не установлен)
sudo apt install screen -y

# Создайте сессию
screen -S referral_bot

# Запустите бота
python3 main.py

# Отключитесь: Ctrl+A, затем D
```

### Вариант C: Запуск через nohup

```bash
nohup python3 main.py > bot.log 2>&1 &
```

## Шаг 5: Проверка работы

1. Откройте вашего бота в Telegram
2. Отправьте `/start`
3. Проверьте, что бот отвечает

## Управление ботом

### Просмотр логов (если использовали nohup)

```bash
tail -f bot.log
```

### Возврат к сессии screen

```bash
screen -r referral_bot
```

### Остановка бота

```bash
# Найти процесс
ps aux | grep main.py

# Остановить (замените PID на реальный)
kill PID
```

## ⚠️ Важно

- Бот должен быть добавлен как администратор в канал
- Убедитесь, что у бота есть права на проверку участников канала
- База данных создается автоматически при первом запуске

## 📞 Поддержка

По вопросам: @Ksodydes

