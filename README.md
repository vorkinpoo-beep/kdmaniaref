# Ksody Design - Реферальный Бот

Мощный и оптимизированный Telegram бот для проведения реферального конкурса.

## 🎯 Функционал

- ✅ Реферальная система с уникальными ссылками
- ✅ ТОП участников в реальном времени
- ✅ Мощная анти-чит система (защита от накрутки)
- ✅ Автоматическая проверка подписки на канал
- ✅ Админ панель для управления ботом
- ✅ Конкурс на 14 дней с призами
- ✅ Оптимизация под слабые серверы (1 ядро, 1GB RAM)

## 🏆 Призы

- 🥇 **1 место** (больше всех рефералов): NFT Snoop Dogg - https://t.me/nft/SnoopDogg-552170
- ⚡ **Первый, кто наберет 100 рефералов**: NFT Instant Ramen - https://t.me/nft/InstantRamen-284068

## 📋 Правила

- Минимальный порог для 1 места: **50 приглашений**
- Конкурс длится **14 дней**
- 1 место получает тот, кто пригласит больше всего друзей
- Приз за 100 рефералов получает тот, кто первым достигнет этой отметки
- Обязательна подписка на канал для засчитывания рефералов
- Система автоматически блокирует подозрительную активность

## 🚀 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/Vorkinpoo-beep/kdmaniaref.git
cd kdmaniaref
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Настройка конфигурации

Откройте файл `config.py` и проверьте настройки:

- `BOT_TOKEN` - токен вашего бота
- `CHANNEL_ID` - ID канала для обязательной подписки
- `ADMIN_ID` - ваш Telegram ID
- Другие параметры при необходимости

### 4. Запуск бота

```bash
python main.py
```

## 🖥️ Запуск в VNC (удаленный сервер)

### Подключение к VNC

1. Подключитесь к вашему VNC серверу через клиент (например, TightVNC, RealVNC)

2. Откройте терминал в VNC сессии

### Установка Python (если не установлен)

```bash
# Для Ubuntu/Debian
sudo apt update
sudo apt install python3 python3-pip -y

# Для CentOS/RHEL
sudo yum install python3 python3-pip -y
```

### Клонирование и настройка

```bash
# Перейдите в домашнюю директорию
cd ~

# Клонируйте репозиторий
git clone https://github.com/Vorkinpoo-beep/kdmaniaref.git
cd kdmaniaref

# Установите зависимости
pip3 install -r requirements.txt
```

### Запуск бота в фоновом режиме

#### Вариант 1: Использование screen (рекомендуется)

```bash
# Установите screen (если не установлен)
sudo apt install screen -y  # или sudo yum install screen -y

# Создайте новую сессию screen
screen -S referral_bot

# Запустите бота
python3 main.py

# Отключитесь от screen (бот продолжит работать): нажмите Ctrl+A, затем D
```

Для возврата к сессии:
```bash
screen -r referral_bot
```

#### Вариант 2: Использование nohup

```bash
nohup python3 main.py > bot.log 2>&1 &
```

Просмотр логов:
```bash
tail -f bot.log
```

#### Вариант 3: Использование systemd (для постоянной работы)

Создайте файл `/etc/systemd/system/referral-bot.service`:

```ini
[Unit]
Description=Ksody Design Referral Bot
After=network.target

[Service]
Type=simple
User=your_username
WorkingDirectory=/home/your_username/kdmaniaref
ExecStart=/usr/bin/python3 /home/your_username/kdmaniaref/main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Активация сервиса:

```bash
sudo systemctl daemon-reload
sudo systemctl enable referral-bot
sudo systemctl start referral-bot
```

Проверка статуса:
```bash
sudo systemctl status referral-bot
```

Просмотр логов:
```bash
sudo journalctl -u referral-bot -f
```

## 🔧 Админ панель

Админ панель доступна только для пользователя с ID, указанным в `config.py` (ADMIN_ID).

### Функции админа:

- 📊 **Статистика** - просмотр общей статистики и ТОП-5 участников
- 🚫 **Забанить пользователя** - блокировка пользователя по ID
- ✅ **Разбанить пользователя** - разблокировка пользователя по ID
- 🔄 **Сбросить конкурс** - сброс даты начала конкурса

### Уведомления

Админ получает уведомления о:
- Новых регистрациях пользователей в боте

## 🛡️ Анти-чит система

Бот автоматически защищает от:

- ✅ Повторных регистраций по одной ссылке
- ✅ Саморефералов (использование своей ссылки)
- ✅ Отписки/подписки для накрутки
- ✅ Подозрительной активности (множественные переключения подписки)
- ✅ Использования ботов и автоматизации

## 📊 База данных

Бот использует SQLite базу данных (`referral_bot.db`), которая создается автоматически при первом запуске.

### Структура БД:

- `users` - информация о пользователях
- `referrals` - связи реферер-реферал
- `subscription_checks` - история проверок подписки
- `subscription_history` - история действий подписки
- `contest_settings` - настройки конкурса

## ⚙️ Оптимизация

Бот оптимизирован для работы на слабых серверах:

- Кэширование проверок подписки
- Минимальное использование памяти
- Эффективные SQL запросы
- Фоновая обработка анти-чита
- Оптимизированные интервалы проверок

## 📝 Структура проекта

```
kdmaniaref/
├── main.py           # Основной файл бота
├── database.py       # Работа с базой данных
├── config.py         # Конфигурация
├── requirements.txt  # Зависимости
├── README.md        # Документация
└── referral_bot.db  # База данных (создается автоматически)
```

## 🔄 Обновление бота

```bash
cd ~/kdmaniaref
git pull origin main
# Перезапустите бота
```

## ❓ Поддержка

По вопросам обращайтесь к администратору: @Ksodydes

## 📄 Лицензия

Проект создан для Ksody Design

